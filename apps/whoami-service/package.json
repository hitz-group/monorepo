{
  "name": "@oolio/api",
  "version": "0.0.0",
  "scripts": {
    "dev": "nodemon --exec go run . --signal SIGTERM --ext js,json,go,mod",
    "build": "go build -o ./dist/api",
    "clean": "rimraf dist",
    "start": "./dist/api"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
